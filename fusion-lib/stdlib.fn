proc sizeof(x)
{
    int length = 0;
    for (i: x)
        length = length + 1;
    return length;
}

proc contains(haystack, needle)
{
    for (cc: haystack)
    {
        if (cc == needle)
        {
            return true;
        }
    }

    return false;
}